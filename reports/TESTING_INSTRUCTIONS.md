# 🧪 Инструкция по Тестированию Data-Driven Отчета

## ✅ Реализация Завершена

Data-driven подход успешно интегрирован в `free_report.html`:

1. ✅ Добавлен `report-engine.js` - универсальный движок генерации
2. ✅ Создан `scenarios-config.json` - конфигурация всех типов похорон
3. ✅ Интегрирован в `free_report.html` - заменена логика генерации
4. ✅ Создан `test-all-types.html` - тестовая страница для всех типов

---

## 🚀 Как Протестировать Локально

### **Вариант 1: Использовать тестовую страницу (рекомендуется)**

1. Откройте в браузере:
   ```
   http://localhost:8000/test-all-types.html
   ```

2. Нажмите на кнопки типов похорон:
   - Traditional
   - Direct Cremation
   - Simple Service
   - Hybrid
   - Burial
   - I'm Not Sure
   - Not Sure (alt)

3. Проверьте:
   - ✅ Отчет загружается для каждого типа
   - ✅ Сценарии генерируются (2 сценария)
   - ✅ Образовательная таблица показывается только для unsure/not_sure
   - ✅ Заголовки обновляются правильно
   - ✅ Нет ошибок в консоли браузера (F12)

### **Вариант 2: Прямые ссылки**

Откройте в браузере:

```
http://localhost:8000/free_report.html?serviceType=traditional
http://localhost:8000/free_report.html?serviceType=direct
http://localhost:8000/free_report.html?serviceType=simple
http://localhost:8000/free_report.html?serviceType=hybrid
http://localhost:8000/free_report.html?serviceType=burial
http://localhost:8000/free_report.html?serviceType=unsure
http://localhost:8000/free_report.html?serviceType=not_sure
```

---

## 📋 Чеклист Проверки

### **Для каждого типа похорон проверьте:**

- [ ] **Конфиг загружается** - нет ошибок в консоли
- [ ] **Сценарии генерируются** - 2 карточки сценариев
- [ ] **Заголовки правильные** - имя услуги отображается корректно
- [ ] **Образовательная таблица:**
  - [ ] Для `unsure` и `not_sure` - показывается
  - [ ] Для остальных типов - скрыта
- [ ] **Тексты сценариев** - соответствуют типу услуги
- [ ] **Рекомендуемый сценарий** - помечен как "RECOMMENDED APPROACH"
- [ ] **Аналитика работает** - события отправляются (если настроено)

---

## 🔍 Отладка

### **Если конфиг не загружается:**

1. Откройте консоль браузера (F12)
2. Проверьте ошибки загрузки `scenarios-config.json`
3. Убедитесь, что файл существует по пути: `reports/config/scenarios-config.json`
4. Проверьте CORS настройки (если тестируете через file://)

### **Если движок не работает:**

1. Проверьте, что `report-engine.js` загружается:
   ```javascript
   console.log(typeof ReportEngine); // Должно быть "function"
   ```

2. Проверьте загрузку конфига:
   ```javascript
   fetch('./config/scenarios-config.json')
     .then(r => r.json())
     .then(config => console.log('Config loaded:', config));
   ```

3. Проверьте генерацию отчета:
   ```javascript
   fetch('./config/scenarios-config.json')
     .then(r => r.json())
     .then(config => {
       const engine = new ReportEngine(config);
       engine.generateReport('traditional');
     });
   ```

### **Fallback режим:**

Если конфиг не загружается, система автоматически переключается на legacy режим (старые функции-генераторы). Это обеспечивает обратную совместимость.

---

## 📊 Структура Файлов

```
reports/
├── free_report.html              # Основной отчет (обновлен)
├── test-all-types.html           # Тестовая страница (новый)
├── config/
│   └── scenarios-config.json     # Конфигурация (новый)
└── js/
    └── report-engine.js          # Движок генерации (новый)
```

---

## 🎯 Что Проверить Особенно

### **1. Тип "I'm Not Sure" (unsure):**
- ✅ Образовательная таблица показывается
- ✅ Сценарии универсальные (не привязаны к конкретному типу)
- ✅ Ссылка "Compare all service types" видна

### **2. Все остальные типы:**
- ✅ Образовательная таблица скрыта
- ✅ Сценарии специфичны для типа
- ✅ Заголовки персонализированы

### **3. Синонимы:**
- ✅ `hybrid` → работает как `simple`
- ✅ `not_sure` → работает как `unsure`

---

## 🚀 Следующие Шаги

После успешного тестирования:

1. ✅ Удалить legacy функции-генераторы (опционально)
2. ✅ Убрать fallback режим (если уверены в стабильности)
3. ✅ Документировать процесс добавления новых типов
4. ✅ Подготовить к миграции в Next.js

---

## 📝 Примечания

- **Локальный сервер:** Запущен на порту 8000
- **Fallback:** Старые функции сохранены для обратной совместимости
- **Конфиг:** Легко редактируется без изменения кода
- **Масштабируемость:** Новые типы добавляются через JSON

---

**Дата:** 2025-01-27  
**Статус:** ✅ Готово к тестированию

